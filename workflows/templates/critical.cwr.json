{
  "name": "roster-critical",
  "_roster_version": "1.1.0",
  "_doc": "Critical mode pipeline: implement → roster-formal-verify → review → ship. E1 downgrade (formal verification declined): roster-run resume routing inserts qa between review and ship.",
  "steps": [
    {
      "kind": "agent",
      "id": "roster-implement",
      "prompt": "Invoke the Skill tool with skill=\"roster-implement\" and args=\"${TASK}\". Pre-hook (run before Skill): TASK=${TASK} node .harness/bin/run-hook.js pre roster-implement. Post-hook (run after Skill): TASK=${TASK} node .harness/bin/run-hook.js post roster-implement.",
      "on_failure": "abort"
    },
    {
      "kind": "agent",
      "id": "roster-formal-verify",
      "prompt": "Invoke the Skill tool with skill=\"roster-formal-verify\" and args=\"${TASK}\". Pre-hook (run before Skill): TASK=${TASK} node .harness/bin/run-hook.js pre roster-formal-verify. Post-hook (run after Skill): TASK=${TASK} node .harness/bin/run-hook.js post roster-formal-verify.",
      "on_failure": "abort"
    },
    {
      "kind": "agent",
      "id": "roster-review",
      "prompt": "Invoke the Skill tool with skill=\"roster-review\" and args=\"${TASK}\". Pre-hook (run before Skill): TASK=${TASK} node .harness/bin/run-hook.js pre roster-review. Post-hook (run after Skill): TASK=${TASK} node .harness/bin/run-hook.js post roster-review.",
      "on_failure": "abort"
    },
    {
      "kind": "agent",
      "id": "roster-ship",
      "prompt": "Invoke the Skill tool with skill=\"roster-ship\" and args=\"${TASK}\". Pre-hook (run before Skill): TASK=${TASK} node .harness/bin/run-hook.js pre roster-ship. Post-hook (run after Skill): TASK=${TASK} node .harness/bin/run-hook.js post roster-ship.",
      "on_failure": "abort"
    }
  ]
}
