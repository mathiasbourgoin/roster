{
  "name": "roster-full",
  "_roster_version": "1.0.0",
  "_doc": "Full mode template (post-plan phases): implement → review → qa → ship. Pre-plan phases (question/research/intake/spec/plan) run before workflow dispatch.",
  "steps": [
    {
      "kind": "agent",
      "id": "roster-implement",
      "prompt": "Execute /roster-implement for task ${TASK}. Read briefs/${TASK}-implementer.md as your sole input. If a pre-hook exists, run: TASK=${TASK} node dist/scripts/run-hook.js pre roster-implement before starting. After completion, run: TASK=${TASK} node dist/scripts/run-hook.js post roster-implement.",
      "on_failure": "abort"
    },
    {
      "kind": "agent",
      "id": "roster-review",
      "prompt": "Execute /roster-review for task ${TASK}. Read briefs/${TASK}-reviewer.md. If a pre-hook exists, run: TASK=${TASK} node dist/scripts/run-hook.js pre roster-review before starting. After completion, run: TASK=${TASK} node dist/scripts/run-hook.js post roster-review.",
      "on_failure": "abort"
    },
    {
      "kind": "agent",
      "id": "roster-qa",
      "prompt": "Execute /roster-qa for task ${TASK}. Read briefs/${TASK}-qa-scope.md. If a pre-hook exists, run: TASK=${TASK} node dist/scripts/run-hook.js pre roster-qa before starting. After completion, run: TASK=${TASK} node dist/scripts/run-hook.js post roster-qa.",
      "on_failure": "abort"
    },
    {
      "kind": "agent",
      "id": "roster-ship",
      "prompt": "Execute /roster-ship for task ${TASK}. If a pre-hook exists, run: TASK=${TASK} node dist/scripts/run-hook.js pre roster-ship before starting. After completion, run: TASK=${TASK} node dist/scripts/run-hook.js post roster-ship.",
      "on_failure": "abort"
    }
  ]
}
