# Iteration 2026-05-22-46: Live Preview First

## Brief

Loop 45 made `Enter` the primary selected-agent action. Loop 46 makes the next
screen useful faster: once an agent is live, the MIAOU main panel shows pane
preview before metadata so session output stays visible on short terminals.

## Product Constraint

TA should remain Herdr-simple. Starting or returning to a live agent should
show the running session first, while roster/source/capability detail remains
available below it.

## Acceptance Criteria

- Detached selected agents keep the existing action, detail, preview order.
- Live selected agents render action, preview, detail.
- Short terminal frames still show the primary action and live pane output.
- No create/connect flows, command selectors, or config forms are added.
- Existing Enter primary-action behavior remains unchanged.

## Result

Completed.

- Added a live-agent layout rule in the MIAOU view.
- Added a headless MIAOU regression test backed by a real tmux pane and state
  snapshot, proving a live pane preview remains visible at `80x10`.
- Kept detached agents metadata-first so roster identity and source remain
  visible before startup.

## Validation

- `opam exec -- dune build @all @runtest`
- tmux-backed headless regression for short live preview

## Review Notes

This loop intentionally changes ordering only. Restart/resume, preview focus
controls, and pane interaction remain future explicit actions.

Generated `index.json` remains unstaged.

## Next Loop Candidate

Add an explicit preview focus mode or preview resize control so live session
inspection can use more of the terminal without hiding the selected agent
identity.
