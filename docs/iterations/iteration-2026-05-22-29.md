# Iteration 2026-05-22-29: Pipeline Edge Traversal

## Brief

Loop 28 made the pipeline topology focusable and navigable by node. Loop 29
adds explicit declared-edge traversal so a future MIAOU page can move from a
pipeline node to its declared ACL handoff and preview the edge target without
inferring workflow order from `pipeline_role` prose.

## Acceptance Criteria

- Add typed edge ids and node-or-edge focus to `Dashboard_topology`.
- Preserve node traversal behavior from loop 28.
- Add declared ACL edge traversal with wrapping.
- Resolve edge targets to visible topology nodes only.
- Render a focused marker for selected edges.
- In pipeline focus, make `Right` select an outgoing edge from the current
  source node when possible; otherwise cycle declared edges.
- Move preview selection to the first visible edge target while preserving edge
  focus.
- Preserve selected edge focus across refresh.
- Keep ordinary `Left`/`Right` agent movement outside pipeline focus.
- Cover topology, interaction, CLI replay, and live tmux/socket behavior.
- Run review, QA, live tmux smoke, commit, and push the loop branch.

## Result

Completed.

- Added private `edge_id` and `focus = Node | Edge` to `Dashboard_topology`.
- Added `move_edge` and `edge_targets` helpers for declared ACL traversal.
- Extended topology rendering so selected edges receive the same focus marker
  as selected nodes.
- Extended `Dashboard_interaction` with selected edge state.
- Pipeline `Right` now prefers the selected node's outgoing declared ACL edge,
  selects that edge, and moves the preview to the first visible target.
- Refresh preserves selected edge focus.
- Added deterministic tests for edge movement, target resolution, focused edge
  rendering, interaction edge selection, refresh preservation, and CLI
  `--key p --key Right`.

## Next Loop Candidate

Add command affordances around focused edges, such as opening source/target pane
metadata in the preview panel or exposing a socket-safe action description for
future MIAOU key bindings.
