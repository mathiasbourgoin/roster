# Iteration 2026-05-22-4: Workspace State Store

## Brief

Add the typed state boundary needed by future supervisor, socket, MCP, and MIAOU
layers. This loop should stay pure and testable: no daemon process, no
persistence format, and no runtime polling yet.

## Acceptance Criteria

- Add a pure state store built from validated TA config.
- Track workspace, agent status, pane attachment, links, and audit events.
- Provide ACL checks over configured links.
- Add tests for config loading, ACL decisions, status audit events, and missing
  agent failures.
- Run review, QA, and tmux smoke before commit.

## Result

Completed.

- Added `State_store`, a pure typed runtime state boundary built from validated
  TA config.
- Tracks workspaces, agents, agent status, pane attachments, configured links,
  ACL decisions, and audit events.
- State transitions validate target workspace/agent and audit actors.
- Audit tests now verify event order, sequence numbers, actors, and payloads.
- Review blockers were fixed before commit.
- QA passed build, tests, formatter check, opam lint, roster validation, tmux
  smoke, and root `npm test`.

## Next Loop Candidate

Add a small JSON snapshot format for `State_store` so the future daemon can
persist/reload workspace state and audit events across restarts.
