# Iteration 2026-05-22-3: Command-Level tactl Tests

## Brief

Close the residual test gap from iteration 2 by exercising the real `tactl`
binary and Cmdliner option wiring.

## Acceptance Criteria

- Add fixture config and roster index files for CLI tests.
- Add an OCaml command-level test that runs the built `tactl` executable.
- Cover plain validation success, `--roster-index` success, and
  `--roster-index` failure for an unknown roster agent.
- Run review, QA, and tmux smoke before commit.

## Result

Completed.

- Added fixture TA configs and a fixture roster index for command-level tests.
- Added `test_tactl_cli.ml`, which runs the built `tactl` executable through
  the real Cmdliner entrypoint.
- Covered plain validation success, `--roster-index` validation success, and
  `--roster-index` rejection for an unknown roster agent.
- Review found no blocking issues; stdout assertions were tightened after a
  reviewer suggestion.
- QA passed build, tests, formatter check, opam lint, root `npm test`, direct
  `tactl` checks, and tmux smoke.

## Next Loop Candidate

Add a persistent workspace state store and audit log so later supervisor,
socket, MCP, and TUI layers have a typed state boundary.
