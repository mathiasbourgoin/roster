# Iteration 2026-05-22-26: Pipeline Overview Section

## Brief

Loop 25 rendered typed `pipeline_role` detail for the selected agent. Loop 26
adds a first whole-dashboard pipeline overview, using only visible agents and
declared ACL links. The section must not imply natural-language
`pipeline_role` text has been resolved into exact workflow graph edges.

## Acceptance Criteria

- Add a pipeline overview section to the static dashboard renderer.
- List visible agents with workspace id, agent id, roster role label, contract
  availability, and current status.
- List declared ACL read/write links under an explicit disclaimer that they are
  not inferred workflow order.
- Preserve compact agent rows and selected preview behavior.
- Keep socket payloads unchanged; socket dashboard renders should show only
  actor-visible agents and ACL edges after client-side roster enrichment.
- Cover model rendering plus state and socket CLI dashboard rendering.
- Run review, QA, live tmux smoke, commit, and push the loop branch.

## Result

Completed.

- Added `Pipeline overview` lines to `Dashboard_model.render`.
- Pipeline rows show `contract` when typed `pipeline_role` metadata is present
  and `unknown` otherwise.
- ACL edge rows are labelled as declared links, not inferred workflow order.
- Existing selected-agent pipeline detail remains in the preview panel.
- Tests prove natural-language pipeline text does not create an edge without a
  declared ACL link.
- Socket render tests and live QA prove actor-scoped redaction hides overview
  rows and ACL rows for non-visible agents.
- Review and QA passed.

## Next Loop Candidate

Turn the pipeline overview into a focusable MIAOU view once the concrete runner
lands, with explicit edge categories for declared ACL edges versus future
structured workflow edges.
