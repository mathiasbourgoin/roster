# Iteration 2026-05-22-37: Miaou Widgets And Local Switch

## Brief

Loop 37 rebases the TA branch on top of latest `origin/main`, makes the
`ocaml/agent-manager` local opam switch compile, and moves the full-screen TUI
from a styled text frame toward actual MIAOU widget composition.

Mathias also clarified the product direction during the loop: starting a Codex
tech lead through hand-written JSON is too complex. The next TUI loop should
make agent start/connect/create flows first-class dashboard actions.

## Acceptance Criteria

- Rebase the current feature branch on latest `origin/main`.
- Create or repair the local opam switch under `ocaml/agent-manager/_opam`.
- Verify TA builds, tests, formats, and resolves `miaou-runner.tui` from the
  local switch.
- Replace the inline `ta_tui.ml` renderer with a MIAOU widget-backed view
  module.
- Preserve short-terminal footer/border behavior and narrow-terminal collapsed
  layout behavior.
- Run tmux Matrix QA from the local switch.
- Record the next TUI-first launch/connect/create milestone.

## Result

Completed.

- Rebased `feat/ta-agent-manager-loop-37` on top of latest `origin/main`.
- Created the local switch with OCaml 5.3.0, installed TA dependencies,
  `miaou-tui`, `alcotest`, and `ocamlformat`.
- Added `ta_miaou_view.ml`, which renders the dashboard through MIAOU
  `Sidebar_widget`, `Table_widget`, and `Description_list`.
- Kept `ta_tui.ml` focused on page lifecycle, key handling, and runner wiring.
- Added headless MIAOU tests for pipeline navigation, short viewport height,
  and collapsed narrow-width behavior.
- Verified `80x10` and `39x18` tmux Matrix smokes from the local switch.
- Installed Herdr 0.6.1 into `/tmp` only and ran it in tmux for comparison QA:
  Herdr creates a workspace with one CLI command, starts a named `tech-lead`
  agent into a split pane, and immediately shows the agent in its sidebar.

## Review Notes

The reviewer found an initial short-terminal overflow and collapsed-sidebar
width regression. Both were fixed before commit by sizing the body to the
MIAOU-provided rows, accounting for `Sidebar_widget` border rows, and using the
full terminal width when MIAOU collapses the sidebar below 40 columns.

## Next Loop Candidate

Implement the TUI-first start workflow: a command palette or modal sequence
where the user chooses workspace, chooses a roster agent, confirms the Codex
profile/command, and TA creates the tmux session plus state snapshot. This
should be followed by TUI ACL connection editing and capability-gated agent
creation through `tech-lead`/`recruiter`.

Herdr comparison raises the bar for that loop: TA should stop exposing JSON as
the normal path. The first screen must be an operational control surface, not a
state inspector.
