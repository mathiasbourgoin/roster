# Iteration 2026-05-22-47: Preview Focus Toggle

## Brief

Loop 46 moved live pane output above metadata. Loop 47 adds an explicit preview
focus toggle so a running agent can use the whole MIAOU body area without
turning the normal dashboard into a configuration-heavy mode.

## Acceptance Criteria

- Pressing `v` toggles preview focus in the MIAOU TUI.
- Preview focus only changes the layout for live selected agents.
- Focused preview keeps the primary action visible.
- Focused preview shows the selected workspace/agent in the preview title.
- Focused preview hides the sidebar and metadata detail so pane output gets the
  full body area.
- Enter start/refresh behavior remains unchanged.
- Static dashboard and `tactl` renderers are unchanged.

## Result

Completed.

- Added MIAOU-local page state with `preview_focus`.
- Added `v` key handling and footer hint.
- Added focused live-preview rendering in `Ta_miaou_view`.
- Extended headless MIAOU coverage to assert focused preview title, pane output,
  hidden sidebar, hidden detail, and bounded height.

## Validation

- `opam exec -- dune build @all @runtest`
- tmux-backed MIAOU headless regression through a real attached pane

## Review Notes

Preview focus is intentionally local to the MIAOU page. It does not alter the
pure dashboard model, socket API, or static renderers.

Generated `index.json` remains unstaged.

## Next Loop Candidate

Make preview focus preserve across refreshes only while the selected live agent
remains the same; otherwise fall back to the normal agent detail view.
