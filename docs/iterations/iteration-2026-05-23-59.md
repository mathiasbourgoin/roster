# Iteration 2026-05-23-59: No Empty Detached Preview

## Brief

Loop 59 removes the remaining empty-preview noise from the detached-agent
first screen.

## Product Constraint

TA should be as simple as Herdr in the common path. Before an agent is started,
the operator needs launch target and authority information, not a terminal
preview placeholder saying no pane output exists.

## Acceptance Criteria

- Detached selected agents render the compact `Launch` panel without an empty
  `Preview` placeholder.
- Attached and live agents still render preview output before detail.
- The pinned footer still owns the primary `Enter Start` or `Enter Refresh`
  action.
- Short and collapsed terminal layouts still keep the launcher visible.

## Result

Completed.

## Validation

- `opam exec -- dune build @all @runtest`
- `opam exec -- dune build @fmt`
- `opam exec -- dune build @doc`
- `npm test`
- `git diff --check`
- tmux-backed MIAOU smoke for a preview-free detached launcher, live attached
  state after `Enter`, refresh availability, and shell pane execution.
- Independent review: no findings.

## Next Loop Candidate

Make privileged create/connect operations discoverable only for authorized
agents, without adding choices for ordinary start/refresh flows.
