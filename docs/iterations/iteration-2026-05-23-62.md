# Iteration 2026-05-23-62: Discoverable Manage Action

## Brief

Loop 62 makes the privileged manage path discoverable at the moment it matters:
when an authorized detached agent is selected.

## Product Constraint

TA should stay as simple as Herdr for ordinary start/refresh. Capability-only
actions should be visible only to agents that can use them, and they should not
compete with the primary `Enter Start` action.

## Acceptance Criteria

- Privileged detached agents show a `Manage` row that names `m proposals`.
- Ordinary agents still do not show `Manage`.
- Pressing `m` still opens the gated `Manage` panel for privileged agents.
- The footer remains focused on `Enter Start`/`Enter Refresh`.
- Existing launch, manage, refresh, and attached diagnostics behavior remains
  unchanged.

## Result

Completed.

## Validation

- `opam exec -- dune build @all @runtest`
- `opam exec -- dune build @fmt`
- `opam exec -- dune build @doc`
- `npm test`
- `git diff --check`
- tmux-backed MIAOU smoke for the privileged launch-panel `m proposals` hint,
  gated manage-panel opening, `Enter` start, live refresh state, and shell pane
  output.
- Independent review: no findings.

## Next Loop Candidate

Persist the first create-agent proposal as an audited draft once the proposal
shape is explicit enough to review.
