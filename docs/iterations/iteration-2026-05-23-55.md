# Iteration 2026-05-23-55: Agent Picker Profiles

## Brief

Loop 55 makes the configured launch profile visible while selecting agents.

## Product Constraint

TA should not offer fake provider/profile choices that do not drive execution.
The first useful step is to show the real profile that Enter will launch for
each agent in the picker itself.

## Acceptance Criteria

- The MIAOU agent table includes a `Profile` column.
- The profile column is derived from the configured command, using existing
  `Launch_profile` classification.
- Selecting a shell-backed agent shows `shell` in the table and footer.
- Selecting a Codex-backed agent still shows `Codex`.
- The footer remains the source of the primary Enter action.
- Runtime, socket, state, and config behavior remain unchanged.

## Result

Completed.

- Added the configured launch profile to each agent row in the MIAOU picker.
- Added headless coverage for Codex and shell profiles in the picker.
- Preserved the selected launch panel and pinned footer behavior.

## Validation

- `opam exec -- dune build @all @runtest`
- `opam exec -- dune build @runtest`
- `opam exec -- dune build @fmt`
- `opam exec -- dune build @doc`
- `npm test`
- `git diff --check`
- tmux-backed MIAOU smoke for selecting a shell-backed agent, complete
  `Enter Start`, live output, and complete `Enter Refresh`

## Next Loop Candidate

Use the profile column as the baseline for a real launch-profile selector only
after profile changes can update the actual start command.
