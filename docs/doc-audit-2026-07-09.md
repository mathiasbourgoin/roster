# Documentation Consistency Audit — 2026-07-09

**Scope:** all documentation (README, AGENTS.md, CHANGELOG/CHANGES, NEXT-SESSION, docs/,
schema/, recruiter/) plus every cross-reference inside skills/ (40 files) and agents/ (24
files), verified against the tree at `17e0d19` (post skill-review batch 99b7c49..17e0d19).
Four independent read-only auditors; ~620 references checked; every claim cited.

**Verdict: code and skills are coherent; the documentation lags the batch in four hot spots.**
6 HIGH, 18 MEDIUM, ~15 LOW findings. The checkers/skills/scripts all agree with each other —
the drift is one-directional: prose that still describes the pre-batch world.

## HIGH (actively misleads an executing agent or user)

| # | Location | Problem | Fix |
|---|---|---|---|
| H1 | `recruiter/ops/update-mechanism.md:210-296` | Cross-repo install fetches only `preamble.md` — knows nothing of the gated fragments. A team installed on an external project gets pipeline skills with **no Pipeline State ledger contract and no friction template** (both now live only in fragments) → durable-state resume and the metabolism loop silently broken on installed projects. Zero hits for `preamble-pipeline|preamble-friction` anywhere in recruiter/ or docs/. | Fetch all three fragments; replicate sync-harness's frontmatter-gated concatenation; fix the line-293 note. |
| H2 | `docs/examples/getting-started.md:15,94-136` | The walkthrough's key teaching moment narrates a fixture defect (silent `return ""`) that commit `581406c` fixed — the fixture now throws, has 4 formatter tests, and the doc's reviewer verdict/outcome are fiction. | Rewrite Steps 3–5 + Outcome around the fixture's real remaining gaps. |
| H3 | `schema/skill-schema.md:17-38` | Documents the inverted description convention ("description = trigger") and forbids `when_to_use` on pipeline skills — the batch's sweep (2a36bc1) made the exact opposite the enforced norm on all 37 skills. A skill authored from this schema fails the current convention. | Rewrite: description = identity clause; when_to_use = triggers (all skills except human-invoked). |
| H4 | `schema/hook-schema.md:141,194,218` | Says skill hooks "are executed by the LLM agent (not a separate process)" and `timeout:` is "advisory, not enforced" — pre-runner world. Reality: run-hook.js executes shell steps with a real AbortController timeout. A hook author relying on "not enforced" gets production `run:` steps killed. | Split semantics by execution owner (runner vs pending_llm_steps). |
| H5 | `skills/pipeline/roster-review.md:114,162` | Specialist table routes TUI scope to `.claude/agents/terminal-ux-reviewer.md` — **no such agent exists anywhere** (not in agents/, projections, or index.json). A TUI review dead-ends. | Ship the agent or replace the row with an existing reviewer + TUI checklist. |
| H6 | `NEXT-SESSION.md` (local, gitignored) | Entire status section predates both July batches ("in flight: roster-upgrade v0.1.0 WIP", "17 skills") — misleads the next session, which is its only purpose. | Rewrite status/in-flight; keep §Versioning + §Working conventions. |

## MEDIUM (stale but recoverable) — grouped

**Batch-conventions never landed in schema/ or README:**
- `schema/skill-schema.md`: single-file preamble injection (:56,:134); "every skill carries the friction template" (:104 — 18 now carry pointers, canonical template is `preamble-friction.md`); required-sections list omits checker-enforced `When to Go Back`/`What Next` while listing non-enforced Input/Output Contract (:113-130); dead Pi runtime (:183); "Claude reads only description" (:15).
- `README.md`: skill-health categories listed as 4 (:138 — now A–F six); AGENTS.md described as hand-maintained (:216 — skill rows are now generated by `populate-catalog-rows.js`).
- `AGENTS.md:229-234`: same auto-generation gap — a hand-edited Purpose cell gets silently reverted on next sync.
- `schema/harness-schema.md:97`: metabolism counter attributed to init/health/evolve — it's incremented by roster-ship; init doesn't bootstrap the block.

**docs/hooks.md internal contradictions (task-11 move merged into older text):**
- `on_error` marked required though optional-with-default (:61); §9 says non-reentrance is "convention only" while §11/runner have a real `ROSTER_HOOK_RUNNING` guard (:539); exit-4 described as guard-only vs also hook-absent (:596); §7 error table lists 11 of 12 enforced checks; §12's moved-in pre-hook steps read as "LLM executes the steps" — must say "invoke run-hook.js, act on exit code" (:608-617).

**Dangling pointers introduced/exposed by the batch (cross-ref audit):**
- `roster-run.md:185,236` + `roster-doctor.md:158`: cite "the preamble's *Pipeline State* section" but neither file receives the pipeline fragment (roster-run has no phase/preamble frontmatter; doctor is phase-null) — the rendered projections contain no such section. Fix: cite `skills/shared/preamble-pipeline.md` explicitly.
- `agents/management/project-auditor.md:75,145,206`: mandates `kb/README.md` as KB root; the whole ecosystem (kb-schema, kb-agent, harness-validator, kb-update, ambiguity-auditor) uses `kb/index.md`.
- `skills/meta/roster-upgrade.md` (6 sites): routes to `/specialize`, which exists nowhere and is never located — unactionable as written.

**Release hygiene:**
- `CHANGES.md`: missing v2.6.2 entry (tag + recruiter/CHANGELOG have it; pre-existing).
- `recruiter/CHANGELOG.md` [2.7.0]: covers only June channel work — both July batches absent; tagging as-is misrepresents the release.
- `recruiter/recruiter.md:474`: "agents cannot spawn other agents — hard platform constraint" contradicts README/AGENTS.md's bounded single-level delegation.
- `docs/agents.md`: 9 KB/audit skills misfiled under "Media skills" (missing heading, :128); 4 shipped skill hooks uncatalogued (:157).
- `docs/proposals/skill-md-unification.md:27-41`: cites the `.opencode` path disagreement as an open blocker; `186ba03` reconciled it.

## LOW (cosmetic — fix opportunistically)

README friction-example casing + 6 pipeline skills missing from its table + spec-phase step count; AGENTS.md:189 "no separate process" wording; recruiter first-run prompts omitting question/research phases; recruiter "Trap:" labels (standing feedback: rename to consistency-check); update-mechanism leftover French + hardcoded v1.0.0; hooks.md exit-1/exit-3 nuances + §12 on_error duplication; extensions.md `list` subcommand + `invalid_targets` undocumented; agents.md formal tier list + personal-overlay contradiction; harness-schema `operational` domain enum unused/disagreeing with skill-schema; agent-schema + schema/README examples; getting-started Node claim + recruiter mock output; roster-implement's unconditional `.claude/agents/ocaml-dune-specialist.md` path (add "if installed" guard).

## Clean bills of health

`docs/integration-verification.md`, `docs/extensions.md` (2 LOW), `docs/skill-overlap.md`,
`schema/profiles.md`, `schema/extension-schema.md`, `schema/kb-schema.md`,
`schema/memory-schema.md`, `schema/rule-schema.md`, `schema/ta-config-schema.md`,
`recruiter/ops/runtime-paths.md`, AGENTS.md all counts/tables (26/37/6 — mechanically green),
README install/pipeline/version-tracks, and the entire cross-reference graph inside skills/:
all 232 skill invocations, all 27 script invocations, all § pointers, all stage/step numbers
(zero stale "Step 1.4/1.5" refs), all tunables declared, all artifact producer/consumer pairs.

## Release-convention answer (asked during audit)

No VERSION/CHANGES bump is due on `next` now — entries are written in `chore(release)` commits
at tag time (precedent: dc5bf98, f6b380f, 6de5723; the 2026-07-02 batch also added none).
Obligations at graduation: amend recruiter/CHANGELOG [2.7.0] + write CHANGES.md 2.7.0 covering
both July batches; next npm 1.x entry must include populate-catalog-rows.js + the hardened
hook checker; backfill CHANGES.md v2.6.2.

## Recommended fix order

1. **H1** (recruit install path — blocks safe external installs) + H5 (dead specialist route).
2. **H3/H4** (schema files — they are the authoring contracts; wrong contracts breed wrong skills).
3. docs/hooks.md contradiction set + dangling preamble-pipeline pointers (one task).
4. H2 getting-started rewrite; README/AGENTS.md auto-generation notes + A–F categories.
5. MEDIUM release-hygiene + LOW sweep (one Express-mode pass each).
