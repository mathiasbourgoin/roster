---
name: roster-spec
type: spec
status: live
feature: critical pipeline route port
brief: briefs/critical-route-port-intake.md
date: 2026-06-14
version: 1.0.0
---

# Spec — Critical Pipeline Route Port

## Clarifications

| Q | A |
|---|---|
| Where is the insertion point in roster-run.md for Step 1.1 and --critical? | After the paragraph ending "Otherwise infer the mode from the signals below." (line 124 on next). This is a content anchor — do not use line numbers. |
| What is the correct CWR step sequence for critical mode? | `roster-implement → roster-formal-verify → roster-review → roster-ship`. Formal-verify gates BEFORE review. Source: roster-formal-verify.md "What Next": "E0p/E0m path: /roster-review (formal-verify replaces QA gate)". |
| Does the LEDGER_SCHEMA byte-identity check use line numbers or content? | Content-based regex: `/LEDGER_SCHEMA='([\s\S]*?)'\n/` in check-pipeline-install.js. Inserting lines before the block does not affect it. |
| Does populate-manifest-skills.js --update handle `phase: null`? | Yes. roster-workflow-build has `phase: null` and is registered — proof that null is accepted. |
| What is the entry format for update-mechanism.md? | `` - `skills/pipeline/<name>.md` `` — matching lines 219–252 in the file. Insert after `roster-doctor.md`, before the blank-line terminator at line ~253. |
| What is the docs/agents.md table format for pipeline skills? | `\| Skill \| Phase \| What it does \|` table. Manual addition required (not generated by sync-harness.sh). |
| Are the three critical skills absent from next's source AND harness? | Confirmed absent from both. skills/pipeline/roster-{triage-critical,spec-formal,formal-verify}.md do not exist on next. harness.json has 35 skills, none of the three. |
| Is roster-qa removed or repositioned in critical.cwr.json? | Removed entirely. No separate /roster-qa on the E0 path per roster-formal-verify.md "What Next". |

## User Stories

### US-1: Three critical skills present and structurally valid on next (Priority: P0)

As a pipeline developer, I want roster-triage-critical, roster-spec-formal, and roster-formal-verify present as source files on `next` with correct frontmatter (`name`, `description`, `version` required; `domain`, `phase`, `capability` present) and all required sections (Steps, When to Go Back, What Next, Friction Log) so that `check-skill-structure.js` passes for all three.

**Why this priority**: Without the source files, populate-manifest-skills.js --update fails, no projections can be generated, and all downstream US-2/US-3/US-4 acceptance criteria are blocked.

**Scope**: This story does NOT cover implementing new formal verification backend logic (Rocq/Quint); it covers only porting the three skill files from main and verifying they are structurally sound on next.

**Independent Test**: `node dist/scripts/check-skill-structure.js` exits 0 with no failures for `roster-triage-critical`, `roster-spec-formal`, `roster-formal-verify`.

**Acceptance Scenarios**:
1. **Given** the three skill files are absent from `skills/pipeline/` on `next`, **When** they are added (from `main`'s `feat(critical-route)` source, reviewed against next's codebase state), **Then** `node dist/scripts/check-skill-structure.js` exits 0 and reports no structure failures for the three new files
2. **Given** a skill file has a missing `## Steps` section, **When** `check-skill-structure.js` runs, **Then** it exits non-zero and names the missing section in its output
3. **Given** the three skills are added, **When** each file is grepped for main-specific version strings or artifact paths that differ on next, **Then** no stale references are found

### US-2: roster-run dispatches --critical route correctly (Priority: P0)

As a pipeline user, I want `roster-run.md` on `next` to include Step 1.1 (Tier A/B suggestion checks — keyword regex on task description, adjacent `.v`/`.qnt` file presence, crypto import scan — emitting `AskUserQuestion` with options `--critical`, `--full`, `--critical=rocq`, `--critical=quint` when any check fires) and `--critical`/`--critical=rocq`/`--critical=quint` flag dispatch (routing to `roster-triage-critical`), inserted after the explicit-mode-override paragraph.

**Why this priority**: Without the dispatch block, a task with `--critical` falls through to the default routing table, routes to the wrong phase, and skips formal verification entirely. Without Step 1.1, critical candidates are never surfaced to the user.

**Scope**: This story does NOT cover the formal verification execution itself; it covers only the routing machinery. It also does NOT modify the LEDGER_SCHEMA predicate (byte-identity enforced by CI).

**Independent Test**: `grep --critical skills/pipeline/roster-run.md` returns matches for both flag-handling and triage-dispatch text; `npm test` passes.

**Acceptance Scenarios**:
1. **Given** `roster-run.md` on next is missing Step 1.1 and `--critical` paragraphs (32-line block from main), **When** they are inserted after "Otherwise infer the mode from the signals below.", **Then** `npm test` passes including `check-pipeline-install.js` Check 4 (LEDGER_SCHEMA byte-identity)
2. **Given** the insertion is made, **When** `grep --critical skills/pipeline/roster-run.md` runs, **Then** it returns matches for both flag-handling and triage dispatch (`roster-triage-critical`)
3. **Given** the LEDGER_SCHEMA block exists at lines 135–150 (shifting after insertion), **When** `node scripts/check-pipeline-install.js` runs, **Then** it exits 0 on Check 4 because the check is content-based (regex), not line-number-based

### US-3: critical.cwr.json uses roster-formal-verify in correct position (Priority: P0)

As a pipeline user, I want `workflows/templates/critical.cwr.json` to have steps `implement → roster-formal-verify → roster-review → roster-ship` (with `_roster_phase1_note` removed and `_doc` updated), so that CWR-dispatched critical workflows execute formal verification before review.

**Why this priority**: The current template has `implement → review → roster-qa → ship`, which skips formal verification entirely and is incorrect for the critical pipeline.

**Scope**: This story does NOT cover adding Rocq/Quint backend CWR templates; it covers only correcting the single `critical.cwr.json` template.

**Independent Test**: `jq -r '.steps[].id' workflows/templates/critical.cwr.json` outputs exactly: `roster-implement`, `roster-formal-verify`, `roster-review`, `roster-ship`.

**Acceptance Scenarios**:
1. **Given** `critical.cwr.json` has steps `implement→review→roster-qa→ship`, **When** fixed, **Then** `jq -r '.steps[].id' workflows/templates/critical.cwr.json` outputs `roster-implement`, `roster-formal-verify`, `roster-review`, `roster-ship` in that order with no additional step IDs
2. **Given** `critical.cwr.json` has `_roster_phase1_note`, **When** the key is removed, **Then** `jq 'has("_roster_phase1_note")' workflows/templates/critical.cwr.json` outputs `false`
3. **Given** the `roster-formal-verify` step prompt needs writing, **When** written, **Then** it includes the pattern `TASK=${TASK} node dist/scripts/run-hook.js pre roster-formal-verify` (pre-hook) and `TASK=${TASK} node dist/scripts/run-hook.js post roster-formal-verify` (post-hook)

### US-4: Three critical skills fully wired into harness (Priority: P0)

As a recruiter/harness user, I want the three skills registered in `.harness/harness.json`, projected to all runtimes, listed in `recruiter/ops/update-mechanism.md`, and documented in `docs/agents.md`, so that `npm test` passes completely.

**Why this priority**: Without harness wiring, the skills are invisible to every runtime, the recruiter install list is stale, and CI's `harness-sync --check` and `check-pipeline-install.js` fail.

**Scope**: This story does NOT cover `.codex/` projections (if the sync script produces them, fine; if not, not a blocker). It does NOT cover tunable customization of the three new skills.

**Independent Test**: `npm test` exits 0 with all three checks passing: `check-skill-structure.js`, `check-pipeline-install.js`, `sync-harness.sh --check`.

**Acceptance Scenarios**:
1. **Given** the three skill files exist at `skills/pipeline/`, **When** `node scripts/populate-manifest-skills.js --update` runs, **Then** `.harness/harness.json` gains exactly three new entries under `layers.skills`
2. **Given** `.harness/harness.json` is updated, **When** `bash scripts/sync-harness.sh` runs, **Then** `.claude/commands/`, `.agents/skills/`, and `.opencode/skills/` each gain projection files for the three new skills
3. **Given** three entries are added to `update-mechanism.md` in the format `` - `skills/pipeline/<name>.md` `` after the `roster-doctor.md` entry, **When** `node scripts/check-pipeline-install.js` runs, **Then** the install-list check exits 0
4. **Given** all changes are applied in sequence (add skill files → populate-manifest --update → sync-harness.sh → git add -A), **When** `npm test` runs, **Then** `check-skill-structure.js`, `check-pipeline-install.js`, and `sync-harness.sh --check` all pass

## Challenges

| ID | Story | Challenge | Resolution |
|---|---|---|---|
| C-1 | US-1 | "Copy from main" ambiguous; no criterion for what constitutes a stale reference | Copy verbatim; grep each file for version strings and artifact paths that differ between main and next; prior research found none |
| C-2 | US-1 | check-skill-structure.js is global; pre-existing failures would block the acceptance scenario | No pre-existing structure failures on next (confirmed by research); acceptance scenario scoped to 3 new files; global npm test must also pass |
| C-3 | US-1 | `preamble` and `friction_log` listed as required in the story — exact types unspecified | Both are optional per `schema/skill-schema.md`; the three skills on main include them; the structure check enforces only `name`, `description`, `version` + required sections |
| C-4 | US-2 | Line-number anchor shifts if any prior edit touches roster-run.md first | Use content anchor: insert after the paragraph containing "Otherwise infer the mode from the signals below." |
| C-5 | US-2 | "Minimal triage brief on shortcut path" undefined | `--critical` (no backend) routes through `roster-triage-critical`. `--critical=rocq`/`--critical=quint` bypass triage and write a minimal placeholder brief; `roster-spec-formal` detects the placeholder and runs property elicitation inline. This shortcut path was present on `main` and preserved on `next`. |
| C-6 | US-2 | LEDGER_SCHEMA check might be line-number-based, breaking after insertion | `check-pipeline-install.js` uses regex `/LEDGER_SCHEMA='([\s\S]*?)'\n/` — content-based; line shift from insertion does not affect it |
| C-7 | US-3 | roster-formal-verify step prompt content is unspecified | Follow established CWR hook pattern: pre-hook invocation + `/roster-formal-verify ${TASK}` instruction + post-hook invocation, matching the structure of `full.cwr.json` steps |
| C-8 | US-3 | `_doc` update content unspecified | Updated to: `"Critical mode pipeline: implement → roster-formal-verify → review → ship."` |
| C-9 | US-3 | roster-qa removed or repositioned? | Removed entirely. Per `roster-formal-verify.md` "What Next": "formal-verify replaces the QA gate; no separate /roster-qa on the E0 path" |
| C-10 | US-4 | harness.json schema might reject `phase: null` | `roster-workflow-build` has `phase: null` and is registered in harness.json — proof that null is accepted |
| C-11 | US-4 | update-mechanism.md exact entry format unspecified | Matches existing entries: `` - `skills/pipeline/<name>.md` `` (lines 219–252 in file) |
| C-12 | US-4 | Execution order unspecified; partial state check failures | Required order: (1) add skill files, (2) populate-manifest --update, (3) sync-harness.sh, (4) git add -A, (5) npm test against final state |

## Functional Requirements

#### Three Critical Skills — Presence & Structural Validity

- **FR-001** [US-1]: The repository MUST contain a file at `skills/pipeline/roster-triage-critical.md` on the `next` branch.
- **FR-002** [US-1]: The repository MUST contain a file at `skills/pipeline/roster-spec-formal.md` on the `next` branch.
- **FR-003** [US-1]: The repository MUST contain a file at `skills/pipeline/roster-formal-verify.md` on the `next` branch.
- **FR-004** [US-1]: Each of the three skill files MUST include frontmatter fields `name`, `description`, and `version`.
- **FR-005** [US-1]: Each of the three skill files MUST include frontmatter fields `domain` and `phase`. (`capability` is optional per `schema/skill-schema.md` and was absent from the canonical source on `main`; not required here.)
- **FR-006** [US-1]: Each of the three skill files MUST contain a `## Steps` section.
- **FR-007** [US-1]: Each of the three skill files MUST contain a `## When to Go Back` section.
- **FR-008** [US-1]: Each of the three skill files MUST contain a `## What Next` section.
- **FR-009** [US-1]: Each of the three skill files MUST contain a `## Friction Log` section.
- **FR-010** [US-1]: When `node dist/scripts/check-skill-structure.js` runs, it MUST exit 0 and MUST NOT report a structure failure for any of the three new skill files.
- **FR-011** [US-1]: Each of the three skill files MUST NOT contain version strings or artifact paths present on `main` but absent or incompatible on `next`.

#### roster-run Dispatch — Critical Route

- **FR-012** [US-2]: `skills/pipeline/roster-run.md` MUST contain a Step 1.1 block that performs keyword-regex matching on the task description (Tier A check).
- **FR-013** [US-2]: Step 1.1 MUST perform a Tier A (deterministic) check for the presence of adjacent `.v` or `.qnt` files in the task context.
- **FR-014** [US-2]: Step 1.1 MUST perform a Tier A (deterministic) check scanning for cryptographic-library imports in the task context.
- **FR-015** [US-2]: When any Tier A or Tier B check in Step 1.1 fires, roster-run MUST emit an `AskUserQuestion` offering the options `--critical`, `--full`, `--critical=rocq`, and `--critical=quint`, with `--full` as default.
- **FR-016** [US-2]: Step 1.1 MUST be positioned in `roster-run.md` immediately after the paragraph ending "Otherwise infer the mode from the signals below."
- **FR-017** [US-2]: `skills/pipeline/roster-run.md` MUST contain a `--critical` flag dispatch block that routes to `roster-triage-critical`.
- **FR-018** [US-2]: `skills/pipeline/roster-run.md` MUST contain `--critical=rocq` and `--critical=quint` flag dispatch blocks. These flags skip `roster-triage-critical` (backend pre-chosen) and instead write a minimal triage brief directly, then proceed to the full pipeline. `roster-spec-formal` detects the placeholder brief and runs property elicitation inline (Stages 2a–2b from roster-triage-critical). This is the canonical shortcut path design from `main`.
- **FR-019** [US-2]: After the Step 1.1 and `--critical` insertions, `npm test` MUST pass including `check-pipeline-install.js` Check 4 (LEDGER_SCHEMA byte-identity).

#### Critical Workflow — CWR Step Order

- **FR-020** [US-3]: `workflows/templates/critical.cwr.json` MUST contain exactly four steps in the order: `roster-implement`, `roster-formal-verify`, `roster-review`, `roster-ship`.
- **FR-021** [US-3]: `workflows/templates/critical.cwr.json` MUST NOT contain a `roster-qa` step.
- **FR-022** [US-3]: `workflows/templates/critical.cwr.json` MUST NOT contain the key `_roster_phase1_note`.
- **FR-023** [US-3]: The `_doc` field in `workflows/templates/critical.cwr.json` MUST describe the step sequence including `roster-formal-verify` and `qa`.
- **FR-024** [US-3]: The `roster-formal-verify` step MUST include a prompt invoking both a pre-hook and a post-hook via `TASK=${TASK} node dist/scripts/run-hook.js pre/post roster-formal-verify`.

#### Harness Wiring — Registration, Projection, Docs

- **FR-025** [US-4]: When `node scripts/populate-manifest-skills.js --update` runs, `.harness/harness.json` MUST gain exactly three new entries under `layers.skills`.
- **FR-026** [US-4]: When `bash scripts/sync-harness.sh` runs after harness.json update, `.claude/commands/`, `.agents/skills/`, and `.opencode/skills/` MUST each gain projection files for the three new skills.
- **FR-027** [US-4]: `recruiter/ops/update-mechanism.md` MUST contain entries `` - `skills/pipeline/roster-triage-critical.md` ``, `` - `skills/pipeline/roster-spec-formal.md` ``, and `` - `skills/pipeline/roster-formal-verify.md` `` after the `roster-doctor.md` entry and before the blank-line terminator.
- **FR-028** [US-4]: `docs/agents.md` MUST contain one row per new skill in the pipeline skills table, covering `roster-triage-critical`, `roster-spec-formal`, and `roster-formal-verify`.
- **FR-029** [US-4]: After all changes, `npm test` MUST pass with `check-skill-structure.js`, `check-pipeline-install.js`, and `sync-harness.sh --check` all exiting 0.

## Acceptance Criteria

- AC-1 [US-1 happy path]: Three skill files added at `skills/pipeline/` → `check-skill-structure.js` exits 0 with no failures for the three new files
- AC-2 [US-1, C-2]: All skill files pass structure check → global `npm test` passes (no pre-existing failures masked)
- AC-3 [US-2 happy path]: Step 1.1 + `--critical` dispatch inserted → `grep --critical skills/pipeline/roster-run.md` returns matches for both flag-handling and triage-dispatch text
- AC-4 [US-2, C-6]: LEDGER_SCHEMA integrity maintained after insertion → `check-pipeline-install.js` exits 0 on Check 4
- AC-5 [US-3 happy path]: correct step order → `jq -r '.steps[].id' workflows/templates/critical.cwr.json` outputs `roster-implement roster-formal-verify roster-review roster-ship`
- AC-6 [US-3, C-9]: roster-qa removed → `jq '.steps | length' workflows/templates/critical.cwr.json` outputs `4` and `jq '[.steps[].id] | contains(["roster-qa"])' workflows/templates/critical.cwr.json` outputs `false`
- AC-7 [US-3, C-8]: `_roster_phase1_note` removed and `_doc` updated → `jq 'has("_roster_phase1_note")' workflows/templates/critical.cwr.json` outputs `false`; `jq '._doc' workflows/templates/critical.cwr.json` contains "roster-formal-verify"
- AC-8 [US-4 happy path]: full wiring complete → `npm test` exits 0

## Edge Cases

- EC-1 [US-1]: One or more skill files already exist on next from a prior partial attempt → verify files are absent before copying; if stale files exist, overwrite with fresh copy from main
- EC-2 [US-1]: A skill file has all required frontmatter keys and section headers but with empty bodies → check-skill-structure.js validates section header presence, not body length; this passes structurally
- EC-3 [US-2]: Both `--critical` and `--critical=rocq` flags in the same invocation → `--critical=rocq` (more specific) takes precedence per the flag-parsing order in the dispatch block
- EC-4 [US-2]: The LEDGER_SCHEMA block content differs from main's (e.g., extra trailing newline) → do not touch lines 135–150; only insert content before that block
- EC-5 [US-3]: critical.cwr.json is malformed JSON before editing → verify `jq empty workflows/templates/critical.cwr.json` exits 0 before making changes
- EC-6 [US-4]: populate-manifest-skills.js --update runs when one of the three entries already exists in harness.json (prior partial wiring) → script is expected to be idempotent; verify by checking entry count after run
- EC-7 [US-4]: skill name collision in .claude/commands/ projections → the three skill names (roster-triage-critical, roster-spec-formal, roster-formal-verify) are unique; no collision risk confirmed by research

## Runnable Checks

- CHECK-1 [AC-1]: `node dist/scripts/check-skill-structure.js 2>&1 | grep -E "roster-triage-critical|roster-spec-formal|roster-formal-verify"` → expected: no failure lines; exit 0
- CHECK-2 [AC-3]: `grep -c "critical" skills/pipeline/roster-run.md` → expected: count ≥ 5 (Step 1.1 + flag dispatch lines)
- CHECK-3 [AC-4]: `node scripts/check-pipeline-install.js` → expected: exit 0, all 4 checks pass including LEDGER_SCHEMA sync
- CHECK-4 [AC-5]: `jq -r '.steps[].id' workflows/templates/critical.cwr.json` → expected: `roster-implement`, `roster-formal-verify`, `roster-review`, `roster-ship` (4 lines, in order)
- CHECK-5 [AC-6]: `jq '[.steps[].id] | contains(["roster-qa"])' workflows/templates/critical.cwr.json` → expected: `false`
- CHECK-6 [AC-7]: `jq 'has("_roster_phase1_note")' workflows/templates/critical.cwr.json` → expected: `false`
- CHECK-7 [AC-8]: `npm test` → expected: exit 0, all checks pass

## Entities

- `roster-triage-critical`: Pipeline skill (`phase: null`) that elicits formal-verification properties from the user, proposes a backend (Rocq/Quint), discloses cost, and writes `briefs/<slug>-formal-triage.md`.
- `roster-spec-formal`: Pipeline skill (`phase: null`) that extends roster-spec output with a formal specification file (`.v` or `.qnt`) derived from the triage brief and the existing spec.
- `roster-formal-verify`: Pipeline skill (`phase: null`) that runs the formal verification backend (coqchk or .itf replay), emits evidence tiers (E0p/E0m/E0m-abstract/E1), and writes `briefs/<slug>-formal-verify.md`. Replaces the QA gate on the E0 path; routes to `/roster-review` on pass.
- `LEDGER_SCHEMA`: The jq predicate in `roster-run.md` Step 1.4 and `roster-doctor status` mode that validates the durable task ledger. Must be byte-identical in both files; enforced by `check-pipeline-install.js` Check 4.
- `critical.cwr.json`: The CWR workflow template for critical-mode tasks. Post-fix step sequence: `roster-implement → roster-formal-verify → roster-review → roster-ship`.
