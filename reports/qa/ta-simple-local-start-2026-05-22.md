# QA: TA Simple Local Start

## Result

Pass so far.

## Automated Checks

- `opam exec -- dune build @all @runtest` passed in `ocaml/agent-manager`.
- `opam exec -- dune build @fmt` passed with the existing ocamlformat warning
  behavior.
- `opam exec -- dune build @doc` passed.
- `npm test` passed from the repository root: 32 node tests passed and 26 source
  agent files passed.
- `git diff --check` passed.

## MIAOU Headless QA

The `ta-cli` suite now includes a temp config with no pre-created state:

```bash
MIAOU_DRIVER=headless ta \
  --config "$config" \
  --workspace smoke \
  --agent lead \
  --tui always
```

The headless input presses `s`, renders, and quits. Evidence asserted by the
test:

- `.ta-state.json` is bootstrapped.
- The selected agent renders `Runtime LIVE`.
- The selected agent renders `Status running`.
- The selected agent renders `attached | r refresh`.

## Socket Regression QA

The socket CLI suite still passes the loop 39 safety checks:

- `start-agent` requires server config.
- `start-agent` requires server actor.
- client `--config` is rejected for `start-agent`.
- client `--actor` is rejected for `start-agent`.
- unauthorized server-bound actors are rejected before launch.

## Manual Tmux Smoke

An isolated temp workspace with a generated `ta.json` and session
`ta-loop40-direct-smoke-<pid>` was exercised with the built `ta.exe` from inside
the temp workspace. The run used no pre-existing state and no socket server.

Evidence:

```text
Runtime       LIVE
Status        running
Pane          %2
Actions       attached | r refresh
- lead [running] roster=tech-lead pane=%2
#2 smoke actor=lead pane lead: %2
#3 smoke actor=lead status lead: not-started -> running
```

## Notes

`index.json` is regenerated by `npm test` and remains intentionally unstaged.
