# QA: TA Selected Agent Start

## Result

Pass.

## Automated Checks

- `opam exec -- dune build @all @runtest` passed in
  `ocaml/agent-manager`.
- `opam exec -- dune build @fmt` passed. The local switch still emits the
  existing ocamlformat warning because no project-local `.ocamlformat` is
  detected.
- `opam exec -- dune build @doc` passed.
- `npm test` passed from the repository root:
  - TypeScript build passed.
  - `build-index.test.js` passed 32 tests.
  - `check-agents.js` passed all 26 source agent files.

## MIAOU Headless QA

Failure command shape:

```bash
tmp=$(mktemp)
opam exec -- dune exec -- tactl state save --output "$tmp" test/fixtures/ta-valid.json
printf '{"cmd":"key","key":"s"}\n{"cmd":"quit"}\n' \
  | MIAOU_DRIVER=headless opam exec -- dune exec -- ta \
      --state "$tmp" --config test/fixtures/ta-valid.json --tui always
```

Evidence: the frame rendered `refresh stale: start-agent requires --socket`,
confirming that `s` dispatches the start action and the failure remains visible
after MIAOU headless idle ticks.

Success command shape:

```bash
opam exec -- dune exec -- tactl socket serve \
  --once --socket "$sock" --state "$state" --config "$config"

printf '{"cmd":"key","key":"s"}\n{"cmd":"render"}\n{"cmd":"quit"}\n' \
  | MIAOU_DRIVER=headless opam exec -- dune exec -- ta \
      --state "$state" --socket "$sock" \
      --workspace smoke --agent lead --tui always
```

Evidence: the frame rendered `Runtime LIVE`, `Pane %0`, and
`Actions attached | r refresh` after pressing `s`.

## Tmux Smoke QA

An isolated temp config was created with session `ta-loop39-smoke-<pid>`, actor
`lead`, target `qa`, and a `lead -> qa` read/write link. The socket server was
started with `--once --config "$config" --actor lead` so it owned the trusted
launch plan and start actor, then the request was sent without any
client-supplied config or actor:

```bash
opam exec -- dune exec -- tactl socket serve \
  --once --socket "$sock" --state "$state" --config "$config" --actor lead

opam exec -- dune exec -- tactl socket request \
  --socket "$sock" --workspace smoke --agent qa start-agent
```

Evidence:

```text
started: smoke/qa
TA state snapshot: 1 workspace(s), 2 agent(s), 2 audit event(s)
panes:
%0 tmux 0
state contains pane:
  - qa [not-started] roster=qa pane=%0
  #2 smoke actor=lead pane qa: %0
```

The temp socket directory and tmux session were removed after the smoke.

## Security QA

- `start-agent` without a server-trusted config returns:
  `start-agent requires socket server --config`.
- `start-agent` without a server-trusted actor returns:
  `start-agent requires socket server --actor`.
- `tactl socket request start-agent` rejects client `--config`, `--roster-index`,
  and `--actor` because those are server-owned trust inputs.
- `start-agent` authorization is checked before launch and again inside the
  locked state update before the pane attachment is persisted.

## Notes

`index.json` was regenerated by `npm test` and remains intentionally unstaged.
