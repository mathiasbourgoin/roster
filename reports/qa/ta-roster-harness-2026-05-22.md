# QA: TA Roster Harness

## Result

Pass.

## Harness Checks

- `bash -n scripts/init-harness.sh scripts/sync-harness.sh` passed.
- `jq '.' .harness/harness.json .claude/harness.json opencode.json` passed.
- `./scripts/sync-harness.sh .` passed and regenerated runtime projections.
- Scratch bootstrap with `./scripts/init-harness.sh <tmp> developer`
  passed with:
  - `.harness/harness.json` present
  - Claude projection in sync with 8 agents
  - OpenCode projection in sync with 7 compatible agents
  - human-validation rule present
- Installed TA harness manifest contains 12 agents, 4 rules, 2 hooks, and 3
  skills.
- `opencode.json` contains 12 generated agents.
- `opencode.json` has no `tech-lead.permission.task` entry; team launch remains
  human-mediated.

## Repo Checks

- `npm test` passed:
  - TypeScript build passed.
  - `build-index.test.js` passed 32 tests.
  - `check-agents.js` passed all 26 source agent files.
- `git diff --check` passed.

## TA Local Switch Checks

All checks used `ocaml/agent-manager/_opam`.

- `opam exec -- dune build @all @runtest` passed.
- `opam exec -- dune build @fmt` passed.
- `opam install odoc -y` installed `odoc` in the local switch.
- `opam exec -- dune build @doc` passed.

## Notes

`dune fmt --check` is not supported by the dune version in this switch, so the
QA gate uses `dune build @fmt`.

`index.json` was regenerated by `npm test` and remains intentionally unstaged.
