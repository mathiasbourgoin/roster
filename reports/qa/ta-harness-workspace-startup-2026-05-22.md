# QA: TA Harness Workspace Startup

## Result

Pass.

## Automated Checks

- `opam exec -- dune build @all @runtest` passed in `ocaml/agent-manager`.
- `opam exec -- dune build @fmt` passed with the existing ocamlformat warning
  behavior.
- `opam exec -- dune build @doc` passed.
- `npm test` passed from the repository root: 32 node tests passed and 26
  source agent files passed.
- `git diff --check` passed.

## MIAOU Headless QA

The `ta-cli` suite now creates a temp workspace containing only
`.harness/harness.json`, then runs:

```bash
MIAOU_DRIVER=headless ta --tui always
```

Evidence asserted by the test:

- The dashboard renders the `agent-roster` workspace.
- The selected detail is `agent-roster/tech-lead`.
- The frame shows `s Start tech-lead`.
- `.harness/ta.json` is generated.
- `.ta-state.json` is bootstrapped.
- Existing `.harness/ta.json` wins over `.harness/harness.json`.
- Invalid harness JSON fails with `invalid JSON`, does not render examples, and
  creates neither generated config nor generated state.

## Tmux Smoke

From the repository root, with no tracked TA runtime config present, an isolated
tmux session `ta-loop42-harness-smoke-<pid>` ran the built MIAOU TUI:

```bash
ta --tui always
```

Captured pane evidence:

```text
TA Dashboard | 1 workspace | 12 agents | live 0 | blocked 0 | failed 0
Workspaces                       s Start tech-lead
> agent-roster 0/12 live
Agent         agent-roster/tech-lead
Roster        tech-lead | id tech-lead
Actions       s Start tech-lead
```

Generated files are ignored:

```text
!! .harness/ta.json
!! .ta-state.json
!! .ta-state.json.lock
```

## Projection QA

The `ta-core` suite checks that harness projection:

- Generates workspace id `agent-roster`.
- Stores root as `..` when output is `.harness/ta.json`, so launches resolve
  from the repository root.
- Uses tmux session `ta-agent-roster`.
- Orders agents as `tech-lead`, `recruiter`, then remaining harness agents.
- Adds coordinator links and validates as a normal `Workspace_config.t`.

## Notes

`index.json` is regenerated by `npm test` and remains intentionally unstaged.
