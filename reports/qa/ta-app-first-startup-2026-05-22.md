# QA: TA App-First Startup

## Result

Pass.

## Automated Checks

- `opam exec -- dune build @all @runtest` passed in `ocaml/agent-manager`.
- `opam exec -- dune build @fmt` passed with the existing ocamlformat warning
  behavior.
- `opam exec -- dune build @doc` passed.
- `npm test` passed from the repository root: 32 node tests passed and 26
  source agent files passed.
- `git diff --check` passed.

## MIAOU Headless QA

The `ta-cli` suite now asserts the TUI-first action affordance in three frames:

- Normal dashboard frame contains `s Start lead`.
- 39-column collapsed dashboard frame contains `s Start lead`.
- Direct-start frame after pressing `s` contains `Runtime LIVE`,
  `Status running`, and `r Refresh | attached`.

## Tmux Smoke

An isolated tmux session `ta-loop41-ui-smoke-<pid>` ran the built MIAOU TUI
against a temp state snapshot created from `test/fixtures/ta-valid.json`.

Captured pane evidence:

```text
TA Dashboard | 1 workspace | 2 agents | live 0 | blocked 0 | failed 0
focus agents | refresh fresh | captured just now
Workspaces                      s Start lead
Agent detail
Status        not-started
Runtime       DETACHED
Actions       s Start lead
q: quit    arrows/jk: move    Tab: focus    p: pipeline    s: start
```

## Documentation QA

- `ta --help=plain` covers `NORMAL TUI FLOW` and pressing `s`.
- `tactl quickstart` covers selecting an agent and pressing `s`.
- `Startup_guide` tests cover automatic `.ta-state.json` creation and the
  advanced CLI fallback command.
- `tactl --help=plain` covers the simple TUI-first flow and starting with
  `dune exec ta`.

## Notes

`index.json` is regenerated by `npm test` and remains intentionally unstaged.
