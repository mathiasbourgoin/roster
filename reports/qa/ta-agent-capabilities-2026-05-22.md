# QA: TA Agent Capabilities

## Result

Pass.

## Automated Checks

- `opam exec -- dune build @all @runtest` passed in
  `ocaml/agent-manager`.
- `opam exec -- dune build @fmt` passed after normalizing the new Dune module
  list indentation. The existing ocamlformat outside-project warnings remain
  unchanged.
- `opam exec -- dune build @doc` passed.
- `npm test` passed from the repository root: 32 node tests passed and 26
  source agent files passed.
- `git diff --check` passed.

## Coverage Added

- Capability parser/serializer, equality, ordering, grant helpers, and unknown
  capability rejection.
- Workspace config parse/roundtrip with explicit capabilities and default-none
  behavior when the field is omitted.
- Harness projection grants `tech-lead` and `recruiter` but not `qa`.
- State snapshot roundtrip preserves capabilities.
- Legacy state snapshots without capabilities parse as `none`.
- Dashboard model propagates capabilities from state.
- MIAOU headless harness startup shows privileged `tech-lead` capabilities and
  non-privileged `qa` capabilities as `none`.

## Tmux Smoke

From the repository root, after removing ignored generated TA runtime files, an
isolated tmux session `ta-loop44-capability-smoke-<pid>` ran the built MIAOU
TUI:

```bash
ta --tui always
```

Captured pane evidence:

```text
TA Dashboard | 1 workspace | 12 agents | live 0 | blocked 0 | failed 0
Agent         agent-roster/tech-lead
Source        harness .harness/harness.json
Privileges    reads 11 | writes 11
Capabilities  create-agent,connect-agents
Actions       s Start tech-lead
```

## Notes

This loop only exposes capability metadata. It does not add create/connect
mutation flows or change existing read/write ACL behavior.

`index.json` was regenerated by `npm test` and remains intentionally unstaged.
